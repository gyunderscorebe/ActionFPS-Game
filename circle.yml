dependencies:
  pre: 
    - sudo apt-get update; sudo apt-get install clang make automake libsdl1.2-dev libogg-dev libsdl-image1.2-dev libopenal-dev libvorbis-dev -y
script: cd source/src && make

test:
  override:
    - cd source/src && make